MEMORY {
    RAM: start = $0000, size = $7000, type = rw, fill = yes;
    ROM: start = $7000, size = $0F00, type = ro, fill = yes;
    WOZMON: start = $7F00, size = $00FA, type = ro, fill = yes;
    RESETVEC: start = $7FFA, size = $0006, type = ro, fill = yes;
}

SEGMENTS {
    BIOS: load = ROM, type = ro;
    WOZMON: load = WOZMON, type = ro;
    RESETVEC: load = RESETVEC, type = ro;
}
